#!/bin/sh
# Public wrapper — delegates to locked implementation.
# The agent can run this but cannot read the locked directory.
set -eu

HERE="$(CDPATH= cd -- "$(dirname -- "$0")" && pwd)"
LOCKED="$HERE/locked/submit-candidate"

exec sudo -n --preserve-env=REPORT_TYPE "$LOCKED" "$@"
