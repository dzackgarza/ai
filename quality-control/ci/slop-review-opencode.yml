name: Slop Review (OpenCode)

on:
  workflow_dispatch:
  schedule:
    - cron: '0 8 * * 4'
  push:
    branches: [main]

jobs:
  slop-review:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      security-events: write
      actions: read

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install tools
        run: bash quality-control/ci/steps/install-tools.sh

      - name: Configure reviewer tools
        run: bash quality-control/ci/steps/configure-slop.sh

      - name: Fetch reviewer context
        env:
          GH_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: bash quality-control/ci/steps/fetch-context.sh ai-slop-review .reviewer-context.md

      - name: Prepare reviewer
        env:
          GITHUB_WORKSPACE: ${{ github.workspace }}
        run: bash quality-control/ci/steps/prepare-reviewer.sh

      - name: Run Slop Review
        env:
          GITHUB_WORKSPACE: ${{ github.workspace }}
          GITHUB_SHA: ${{ github.sha }}
          GITHUB_BASE_REF: ${{ github.base_ref }}
          PR_NUMBER: ${{ github.event.number }}
        run: |
          bash quality-control/ci/steps/run-review.sh \
            slop \
            quality-control/reviews/slop/template.md \
            slop

      - name: Collect review output
        env:
          GITHUB_WORKSPACE: ${{ github.workspace }}
        run: bash quality-control/ci/steps/collect-review-output.sh

      - name: Convert report to SARIF
        env:
          GITHUB_WORKSPACE: ${{ github.workspace }}
        run: bash quality-control/ci/steps/convert-sarif.sh ai-slop-review

      - name: Upload review findings
        uses: github/codeql-action/upload-sarif@v4
        with:
          sarif_file: .review-report.sarif
          category: ai-slop-review

      - name: Check Slop Threshold
        if: ${{ vars.SLOP_FAIL_BELOW != '' }}
        run: |
          threshold="${{ vars.SLOP_FAIL_BELOW }}"
          tier1=$(jq '[.findings[] | select(.tier == "tier1")] | length' .review-report-artifact.json)
          tier2=$(jq '[.findings[] | select(.tier == "tier2")] | length' .review-report-artifact.json)
          score=$((100 - tier1 * 20 - tier2 * 5))
          [ "$score" -lt 0 ] && score=0
          if [ "$score" -lt "$threshold" ] 2>/dev/null; then
            echo "Slop Health Score $score is below threshold $threshold"
            exit 1
          fi
