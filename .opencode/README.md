# NOTE

This is NOT a global opencode configuration - this is the configuration *for this specific repo*. 
Opencode automatically creates it when you open it in a directory, so its existence can not be helped.
In any case, one should not use this directory for anything whatsoever -- all configuration is global.
