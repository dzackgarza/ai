---
name: git-guidelines
description: Use for any Git or GitHub operation. Routes to the smallest procedure
  for the requested repository action.
---
# Git and GitHub Work

Start here for Git or GitHub work. Load the operational baseline, then the one leaf matching the task:

- [[git-guidelines/operations/operations|operations]] — staging, commits, deletion safety, and checkpoints.
- [[git-guidelines/auth/auth|authentication]] — GitHub credentials and `gh` login.
- [[git-guidelines/repositories/repositories|repositories]] — clone, fork, remotes, and releases.
- [[git-guidelines/issues/issues|issues]] — create or manage issues.
- [[git-guidelines/pull-requests/pull-requests|pull requests]] — create, update, or merge a PR.
- [[git-guidelines/review/review|code review]] — inspect a PR.
- [[git-guidelines/feedback/feedback|review feedback]] — triage and act on returned feedback.
- [[git-guidelines/request-review/request-review|request review]] — prepare work for review.
