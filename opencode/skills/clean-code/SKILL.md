---
name: clean-code
description: Use when legacy prompts request clean-code or older guidance names this skill; redirects code style and refactoring work to the canonical code-patterns skill.
---
# Clean Code Redirect

Canonical guidance moved to `code-patterns`.

Load `code-patterns` for cross-language style rules, then load the relevant language
subskill such as `code-patterns-python` for Python work.
