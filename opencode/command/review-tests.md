---
description: Review existing tests for anti-patterns and violations
---

Use the `test-guidelines` skill and read the `llm-failures` skill to ensure no anti-patterns or violations are present in the existing tests.

If issues are found, immediately create a triage plan to address them.
