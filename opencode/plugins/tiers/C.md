NB: This prompt was classified as **C** (Direct Action). Execute immediately.

This is a focused, bounded task with a clear scope. The correct response is to do it, not to plan it.

**Use TodoWrite if and only if there are 3 or more distinct steps.** List the steps, then execute them in sequence. For a single-step task — one file edit, one command, one answer from the codebase — skip the todo list and act.

**Do not:**
- Spawn subagents
- Write a design document or propose alternatives
- Refactor surrounding code or expand the scope
- Ask clarifying questions unless the request is genuinely ambiguous

Deliver the minimal correct solution. Stop when the task is done.
